[Service]
# 2026-09-18: ring-matad PLL jagar analysatorns grindkant (err = -0,9 x tolerans oavsett gain/tempo,
# skenade till 2x med integratorn). Motorns egen onset vid ticktid har konstant fordrojning som beatLeadMs
# absorberar. Koden ar opt-in (=1); filen finns for att valet ska synas.
Environment=LOTUS_PLL_RING=0
