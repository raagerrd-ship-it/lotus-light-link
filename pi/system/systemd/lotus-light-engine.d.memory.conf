[Service]
# Höjd 2026-08-29 efter att sonos-buddy + cast-away flyttats till brew-Pi:n.
# MemoryMax var 224 MB = EXAKT samma som V8:s max-old-space-size. Farligt:
# max-old-space styr bara old space, inte total RSS (+ new space, kod, native,
# buffertar). Om V8 nådde sitt tak hade cgroupen OOM-dödat processen.
# Heap-gränsen lämnas på 224 — motorn använder 105 MB och är inte i närheten.
MemoryMax=300M
